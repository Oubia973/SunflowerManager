# Project Architecture

This is a React application with:

- Components in `/src/tables`
- API routes in `../sflman/assets/api`
- State management using Redux in `/src/store`
- Options in `foptions.js`